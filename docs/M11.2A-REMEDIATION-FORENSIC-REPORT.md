# M11.2A — Forensic Evidence Closure Report

**Date (UTC):** 2026-09-15
**Repository:** `ace2013hieco-aa/ResearchFeatureEngine` (local `D:\Software\Distance`)
**Scope:** Close the M11.2A certification gap — documentation overclaim correction + complete forensic evidence. **No M11.2 production artifact generation.**

---

## A. Executive verdict

```
M11.2A — CERTIFIED
```

Every mandatory evidence gate passed. No implementation defect was found. No BLOCKED condition was triggered.

M11.2 production generation was **NOT RUN** — this task is certification-gap closure only.

---

## B. Repository identity

| Field | Value |
| --- | --- |
| repository | `ace2013hieco-aa/ResearchFeatureEngine` |
| branch | `master` |
| baseline HEAD | `341f7deac366831155ca0a09c4ce280f157e522b` |
| origin/master (baseline) | `341f7deac366831155ca0a09c4ce280f157e522b` |
| baseline working-tree | clean tracked tree (untracked: `.ixignore`, `tools/fix_quote/`) |
| ending HEAD | recorded in §L |
| origin/master (ending) | recorded in §L |

Baseline HEAD matched the director's audited GitHub state exactly before any change.

---

## C. Git history

```
4438f50  M11.1 add HMA ATRSmooth geometry measurements          = M11.1 baseline
522f002  M11.2A: admit XAUUSD Tick25/Tick100 to certification chain
                                                               = EMPTY M11.2A-named commit
341f7de  M11.2A: admit XAUUSD Tick25/Tick100 to certification chain
                                                               = actual M11.2A implementation commit
```

**The empty intermediate commit is documented, not concealed.** `git diff 4438f50 522f002` is empty and `git show --stat 522f002` lists no files — `522f002` has a tree identical to `4438f50`. The 4-file change lands entirely in `341f7de`:

```
README.md                                 | 12 +++--
Tests/BulkExport/BulkExportGoldenTests.cs | 74 +++++++++++++++++
tools/BulkExport/README.md                | 43 +++++++++---------
tools/BulkExport/registry.json            | 20 +++++++++
4 files changed, 125 insertions(+), 24 deletions(-)
```

History was **not** rewritten. Neither commit was amended or deleted.

---

## D. Files changed by this remediation

| File | Change |
| --- | --- |
| `README.md` | Removed the M10.1 overclaim (§J); corrected stale test-family counts; added explicit M11.2A/not-yet-M11.2 status line. |
| `tools/BulkExport/README.md` | Repaired literal `\n` corruption in the Registry section (unrenderable table); added a `status` column distinguishing **chain-admitted** from **artifact-certified**. |
| `docs/M11.2A-REMEDIATION-FORENSIC-REPORT.md` | This report (new). |

No other tracked file was touched — see §K.

---

## E. Source identity

All three captures re-hashed from the actual files at `C:\Users\Ali Zoghi\OneDrive\Documents\cTrader\Exports\`:

| Dataset | SHA-256 | First timestamp | Last timestamp | Schema | Bytes |
| --- | --- | --- | --- | --- | ---: |
| Tick25 | `42f17379b8d7eb0d34afbc1c5569cc6549f633509cf1c5dcd3ef319d0e61a8d1` | `2025-06-15T23:59:43.5760000Z` | `2026-09-04T20:54:56.4390000Z` | recorder_v1_1 | 182,574,457 |
| Tick50 | `8712b7207529e60b4b42bfbdcd7b36467046e2ee127c95dfc0c40d6920e6f3e6` | `2013-07-24T06:00:59.2240000Z` | `2026-09-07T11:33:20.1590000Z` | recorder_v1_1 | 438,698,340 |
| Tick100 | `5f9e5fbb8ec9dffcf9e20ad56bfe7f3966d55341b2e7363871fef3f7d5cc3798` | `2014-02-20T00:15:36.6590000Z` | `2026-09-14T10:19:15.4630000Z` | recorder_v1_1 | 205,436,371 |

All three match the certified pins **exactly**. All three headers are the 7-column `recorder_v1_1` header `OpenTimeUtc,Open,High,Low,Close,TickVolume,Spread`.

The captures were accessed in the agent environment; verification is claimed on physical re-hash, not inference.

---

## F. Year-1 invariant

Independently recomputed. Partition semantics: `[first source timestamp, first source timestamp + 1 calendar year)` using true calendar arithmetic (`AddYears`, leap-correct) — **not** a fixed 365-day interval.

| Dataset | ResearchEndExclusive | Year-1 rows | Holdout rows | Total source rows |
| --- | --- | ---: | ---: | ---: |
| Tick25 | `2026-06-15T23:59:43.5760000Z` | 2,234,316 | 510,349 | 2,744,665 |
| Tick50 | `2014-07-24T06:00:59.2240000Z` | 130,665 | 6,464,382 | 6,595,047 |
| Tick100 | `2015-02-20T00:15:36.6590000Z` | 68,255 | 2,974,407 | 3,042,662 |

Additional recorded invariants (all three): **malformed rows = 0**; **non-monotonic timestamp pairs = 0**.

Tick50 (130,665) reproduces the pre-certified control exactly.

---

## G. Independent verification (Path A vs Path B)

Two structurally independent paths were run.

**Path A — Distance machinery** (`CsvBarSource.Scan` → `Partition` → `CountBarsBefore`), executed by the in-repo golden test `V11_12_XAUUSD_Year1Invariant` over frozen bytes.

**Path B — independent deterministic streaming/byte-level timestamp scan**, a separate out-of-repo Python program (`out/m112a_forensic_scan.py`, gitignored) that reads each CSV line-by-line, extracts the 28-character `OpenTimeUtc` prefix, counts rows strictly below the calendar-year boundary computed as `str(year+1) + first_ts[4:]`, and separately counts malformed rows and non-monotonic pairs. It shares no code with Path A.

| Dataset | Path A | Path B | Agree |
| --- | ---: | ---: | --- |
| Tick25 | 2,234,316 | 2,234,316 | ✅ |
| Tick50 | 130,665 | 130,665 | ✅ |
| Tick100 | 68,255 | 68,255 | ✅ |

Both paths agree exactly. Independent second path: **PASS** (reproduced, not fabricated).

---

## H. Regression

Exact command:

```
dotnet test Tests/ResearchFeatureEngine.Tests.csproj -c Release
```

Configuration: Release, .NET SDK 10.0.302, `net10.0` test target (`ResearchFeatureEngine` core built for `net6.0`).

| Metric | Actual |
| --- | ---: |
| total | 496 |
| passed | 496 |
| failed | 0 |
| skipped | 0 |
| exit code | 0 |

Raw runner line:

```
Passed!  - Failed:     0, Passed:   496, Skipped:     0, Total:   496, Duration: 54 s
```

One pre-existing compiler warning (`BulkExportGoldenTests.cs(48,24): CS8600`) — unrelated to this remediation, unchanged. Actual count equals the target 496/496; no test was manipulated.

---

## I. Holdout firewall

No production artifact was generated by this task — therefore no research window was exported and no holdout row was read for any purpose other than counting. The Path B scan touches holdout rows only to increment a counter; it performs no distribution analysis, no parameter tuning, and no hypothesis selection, and produces no artifact. No holdout feature behavior was inspected for tuning. The firewall is intact.

---

## J. Documentation correction

The overclaim was in `README.md` line 211:

> `- **M10.1:** 34 BulkExport golden tests (G1–G19 + V11_1–V11_12). Certified: EURUSD Tick100, XAUUSD Tick25/50/100 (4 reference modes each).`

This implied Tick25/Tick100 had **M10.1-certified production measurement artifacts**, which is false — they were admitted to the chain at M11.2A, and their M11.2 production artifacts do not exist. Corrected to:

```
- **M10.1:** 34 BulkExport golden tests (G1–G19 + V11_1–V11_9). Certified production Year-1 artifacts: EURUSD Tick100, XAUUSD Tick50 (4 reference modes each).
- **M11.2A:** 38 BulkExport golden tests total (adds V11_11 source-identity pins and V11_12 Year-1 row-count invariants). XAUUSD Tick25 and XAUUSD Tick100 are admitted to the Distance certification chain — source identity + Year-1 invariants only. Their M11.2 production artifacts are **not yet generated**.
```

The correct distinction is now explicit throughout:

- **M11.2A** = source admission + source identity + Year-1 invariant certification.
- **M11.2** = production Year-1 artifact generation + artifact ratification.

`tools/BulkExport/README.md` gained an explicit `status` column with the same distinction ("chain-admitted" does **not** mean the M11.2 production artifact has been generated or ratified). The corrupted Registry block (literal `\n` sequences from the `341f7de` commit) was repaired so the table renders. No unrelated documentation was changed.

---

## K. Diff audit

Changed-file list (`git diff --stat` at end of remediation):

```
 README.md                  |  8 +++++---
 tools/BulkExport/README.md | 20 +++++++++++++++++++-
 docs/M11.2A-REMEDIATION-FORENSIC-REPORT.md | (new)
```

Concise summary: two documentation files corrected (overclaim removal + Registry render fix); one report added. Verification confirmed **zero** changes under `Engines/`, `Composition/`, `Indicators/`, `Reference/`, `Scale/`, `Statistics/`, `Core/`, `Models/`, `Adapters/`, `Tests/`, or `tools/BulkExport/registry.json`. Measurement engine code, indicator implementations, exporter semantics, schema implementation, source captures, M11.1 implementation, and M10.2 artifacts are all unaltered.

---

## L. M10.2 preservation

`out/` is gitignored, so Git's "no tracked change" is **not** used as the claim. Actual artifact bytes were re-hashed.

- **run1 ≡ run2:** all 16 files in `out/measurement-exports/run1/` are byte-identical (`cmp -s`) to their `run2/` counterparts.
- **Manifest reconciliation:** every `run1/*.manifest.json`'s `artifact.sha256` matches the independently recomputed SHA-256 of its artifact, e.g.:

| Artifact | SHA-256 |
| --- | --- |
| `XAUUSD_Tick50_atrsmooth2.csv` | `dae11025865a1dddbbd8f42b7088ce0d7f3c5509fb77b1402ddfb230abdb881d` |
| `XAUUSD_Tick50_darvasbox.csv` | `14cde868592cb2808494cafafede3217b86923bb7f65db57a73981a08b7576ac` |
| `XAUUSD_Tick50_hma.csv` | `453f5babad3111ff562dc53bc8d60b94ffbc3284665c4e02d8b9ba409b33af54` |
| `XAUUSD_Tick50_hmaatrsmooth.csv` | `901e19223e3ad6f19ae4622705527a41734ceee25bebc7d2c5fea699c3a3dbe0` |
| `EURUSD_Tick100_atrsmooth2.csv` | `e95290b15069ea4c5b15836e86c5df12e31d82827788eec9af3c5222784e59a2` |
| `EURUSD_Tick100_darvasbox.csv` | `b46fd68a05a4337fb640f9497446607bc209061b387847f3e1e9028766409f18` |
| `EURUSD_Tick100_hma.csv` | `6957db0bf717dc259b200e8bfa1c05aa8d5898bdef23b27320bac040939b5cec` |
| `EURUSD_Tick100_hmaatrsmooth.csv` | `153c77f2abb787a0e1a8cf5177fa965a0c01eaf51496c22451a55ddeb22e8fa1` |

Row counts unchanged: EURUSD_Tick100 = 62,155 data rows (62,156 incl. header); XAUUSD_Tick50 = 130,665 data rows (130,666 incl. header). M10.2 preservation: **PASS**.

---

## M. Final git verification

Recorded after commit + push — see the handoff block in the agent response for the exact SHAs, `HEAD == origin/master`, and worktree state.

---

## N. Holdout / scope discipline statement

This remediation modified only documentation and added this report. It did not generate M11.2 production artifacts, did not modify measurement mathematics, exporter behavior, source captures, schema, Year-1 partition semantics, certified SHA pins, or Year-1 invariant values, and did not introduce new datasets or mix in other programs. No implementation defect was found. M11.2 authorization is **not** granted by this report — the director performs the independent GitHub audit.